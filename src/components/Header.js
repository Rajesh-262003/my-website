import React from 'react';
import './Header.css';

function Header() {
    return (
        <header className="header">
            <h1>Connection with Image</h1>
            <p>Your memories preserved with every note and image</p>
        </header>
    );
}

export default Header;
